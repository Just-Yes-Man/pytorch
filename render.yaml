services:
  - type: web
    name: ae-pytorch-flask
    env: python
    buildCommand: pip install -r requirements.txt && python ae_3_2_3_sigmoid_pytorch.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
